Review the following BGP configuration against the bgp-best-practices skill.

Configuration to review:

$ARGUMENTS

For each best practice category (peer groups, route filtering, prefix limits, BFD,
authentication, graceful restart, route dampening, logging, TTL security, communities,
default route origination), report:

- **Compliant** — the config follows this practice
- **Non-compliant** — the config violates this practice (explain what to fix)
- **Not applicable** — this practice does not apply to this config

End with a summary of findings and a prioritized list of recommended changes.
